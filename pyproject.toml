[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.mypy]
strict = true

[tool.ruff]
select = [
    "ALL",
]
ignore = [
    "ANN101",
    "ANN102",
    "ANN401",
    "D",
]
target-version = "py38"

[tool.ruff.format]
preview = true
